{
  "name": "ai-agent-radar",
  "version": "1.0.0",
  "private": true,
  "description": "Professionally filtered open-source AI agent intelligence from GitHub, powered by Vercel Serverless and Vercel KV.",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check app.js && node --check validation.js && node --check workspace.js && node --check lib/radar.js && node --check lib/opportunity-themes.js && node --check api/get-agents.js && node --check api/fetch-agents.js && node --check api/agent.js && node --check api/weekly.js && node --check api/opportunities.js && node --check api/opportunity.js && node --check api/patterns.js && node --check api/compare.js && node --check api/sitemap.js && node --check api/feed.js"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  },
  "devDependencies": {
    "vercel": "^48.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
